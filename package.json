{
  "name": "edgeroute-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Edge-Ready AI Semantic Router & Evaluation Framework",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@edgeroute/core && npm run build --workspace=@edgeroute/server && npm run build --workspace=@edgeroute/ai",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
